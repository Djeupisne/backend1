server.py appears to be a source code file, which should not be ignored according to rule 7. Since there are no build artifacts, dependencies, or temporary files in the changes, no additional rules need to be added.

(empty)